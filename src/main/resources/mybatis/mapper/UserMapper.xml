<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.petwear.mapper.UserMapper">

    <select id="queryUserList" resultType="User">
        select * from pet_wear.users
    </select>

    <select id="loginByPhone" resultType="java.lang.String">
        select user_name from pet_wear.users where user_phone = #{phone} and user_password = #{password}
    </select>


</mapper>